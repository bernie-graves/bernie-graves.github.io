name: Upload artifact
on:
  push:
    branches: [ main ]
jobs:
  - name: Download a Build Artifact
    uses: actions/download-artifact@v2.0.10
    with:
      # Artifact name
      name: # optional
      # Destination path
      path: # optional
